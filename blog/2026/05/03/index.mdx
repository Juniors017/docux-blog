---
title: "How to Use an API at Build Time in Docusaurus"
slug: fetch-api-build-docusaurus
description: "Simple example: fetch an API at build time, generate static JSON and display photos in Docusaurus."
schemaTypes: ["HowTo", "BlogPosting"]
image: "/img/api.png"
authors: ["docux"]
mainTag: api
tags: ["docusaurus", "api", "build", "react", "static", "photos"]
date: 2026-05-03
difficulty: "Intermediate"
yield: "Use an API at build time in Docusaurus"
genre: "Technical Tutorial"
inLanguage: "en"
copyrightYear: 2026
copyrightHolder: "Docux"
keywords: ["docusaurus", "api", "build", "photos", "json", "static site"]
hide_table_of_contents: false
series: "Api and scripts in Docusaurus"
blueSkyRecordKey: 
---

<center>
<div>
[![Developer](https://img.shields.io/badge/Developer-Docux-green.svg)](https://github.com/Juniors017)
[![Development](https://img.shields.io/badge/Development-passed-green.svg)](https://github.com/Juniors017)
[![License: MIT](https://img.shields.io/badge/License-MIT-yellow.svg)](https://opensource.org/licenses/MIT)
[![AI 50%](https://img.shields.io/badge/AI-50%-blue.svg)](https://github.com/Juniors017)
</div>

![](/img/api.png)
</center>


## Goal

I show here a simple and concrete example: using an API during Docusaurus build, saving the result in a static JSON file, then displaying this data in a page.

This type of flow is ideal for keeping a static site in production while leveraging external data at compile time.

## Project Architecture

<Trees>
  <Folder label="scripts" expanded={true}>
    <File label="return-api-data.js" icon="javascript" />
  </Folder>
  <Folder label="static" expanded={true}>
    <Folder label="json"  expanded={true}>
      <File label="photos-data.json" icon="json" />
    </Folder>
  </Folder>
  <Folder label="src" expanded={true}>
    <Folder label="components" expanded={true}>
      <Folder label="PhotosGrid" expanded={true}>
        <File label="index.jsx" icon="javascript" />
      </Folder>
    </Folder>
    <Folder label="pages" expanded={true}>
      <File label="photos.jsx" icon="javascript" />
    </Folder>
  </Folder>
</Trees>

## Create the Build Script

The script `scripts/return-api-data.js` queries the following API:

```js
const API_URL = 'https://api.slingacademy.com/v1/sample-data/photos?offset=5&limit=20';
```

It fetches the data and writes it to `static/json/photos-data.json`:

<Snippet filename="scripts/return-api-data.js" source="scripts/return-api-data.js" />

This script is executed before the build. It allows generating a static file that will be directly served by Docusaurus.

## Add the `prebuild` Hook

In `package.json`, we add:

```json
"scripts": {
  "prebuild": "node scripts/return-api-data.js",
  "build": "docusaurus build"
}
```

With this configuration, `npm run build` first launches the fetch script, then compiles the site.

## Display Photos in a Page

The page `src/pages/photos.jsx` reads the static JSON and displays the photos.

<Snippet filename="src/pages/photos.jsx" source="src/pages/photos.jsx" />

The page loads `/json/photos-data.json`. If this file doesn't exist in development, it falls back to direct API loading to still display images.

See the page in action [/photos/](/photos/)

## Create the `PhotosGrid` Component

The component `src/components/PhotosGrid/index.jsx` is the element that renders the photo grid in your article.

It does several important things:

- it first tries to load the static file `/json/photos-data.json`
- if this file doesn't exist in development, it automatically falls back to the Sling Academy API (our demo API)
- it manages loading state (`loading`), errors, and the case where no photos are available
- it limits the number of displayed photos with the `limit` prop
- it builds a responsive grid with Docusaurus cards

<Snippet filename="src/components/PhotosGrid/index.js" source="src/components/PhotosGrid/index.js" />

The component is exposed in MDX via `src/theme/MDXComponents.js`, which allows calling it directly in an article with:

```html
<PhotosGrid limit={6} />
```

This creation is a practical option: it allows cleanly inserting data into an MDX page without duplicating the fetch and display logic.

## Preview of Retrieved Data

Here is a preview of the JSON file generated by the build script:

<Snippet filename="static/json/photos-data.json" source="static/json/photos-data.json" />

The file contains 20 photos with their metadata (URL, title, description, user ID).

## Final Result

After the build, the `/photos/` page or the `PhotosGrid` component display a beautiful grid of photos fetched from the API:

### Page Visual Rendering

The page presents:

- **Page Title**: "Photos from the Test API"
- **Page Description**: Explanation of how it works (static file vs direct API)
- **Responsive Grid**: 4 columns on desktop, adapts on mobile
- **Individual Cards** for each photo with:
  - Full-width image
  - Photo title
  - Short description
  - Consistent Docusaurus styling

### Live Preview of Photos

Here is a **live** preview of photos fetched from the API (limited to 6 photos):

<PhotosGrid limit={6} />

*The photos above are loaded dynamically from the JSON file generated at build time or directly from the API in development mode.*

### Features

- **Asynchronous Loading**: Loading indicator during retrieval
- **Error Handling**: Error message if API fails
- **Smart Fallback**: Automatic switching between static file and API
- **Responsive Design**: Adapts to all screens

The final result is a completely static page that displays instantly! ⚡

## Why It's Interesting

- you preload data at build time
- you avoid making API requests on the client side in production
- you can deploy to a static host without a backend server

## Simple Alternative

If you want, you can replace the React page with an MDX page, but the principle remains the same:

1. fetch the API before the build
2. save data in `static/`
3. consume this static file in your page

> This article shows how to use an API at build time with a light and easy-to-reproduce example.

