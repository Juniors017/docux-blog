# Dependencies
/node_modules

# Production
/build
/static/img/old/
.draft
# Brouillons et notes de travail (déjà exclus du build : Docusaurus ignore les
# dossiers commençant par un point). Sauvegardés par OneDrive, pas par Git.
/blog/.draft/

# Cadrage Claude Code — personnel, hors du dépôt public. Le dossier entier (skills, sous-agent,
# gabarit, contextes de chantier, archive) et la charte racine restent sur le disque, sauvegardés
# par OneDrive. Rien n'en a jamais été publié : à ne pas remettre dans Git.
/.claude/
/CLAUDE.md

# Generated files
.docusaurus
.cache-loader

# Misc
.DS_Store
.env.local
.env.development.local
.env.test.local
.env.production.local
.env

npm-debug.log*
yarn-debug.log*
yarn-error.log*
